{% macro current(route, target, exact) %}
    class="{% if (exact|default(false) and route == app.request.get('_route')) or ( not exact|default(false) and route in app.request.get('_route')) %}active {% endif %}item"
    {% set target = target|default(route) %}
    href="{{ path(target) }}"
{% endmacro %}
