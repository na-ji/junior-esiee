{% extends '::layout.html.twig' %}

{% block title 'Confirmation de clôture' %}

{% block content %}
	<div class="col-sm-12">
		<h1>Voulez-vous vraiment clôturer "{{ project.title }}" ?</h1>
	    <a class="btn btn-lg btn-warning" href="{{ path('je_business_project_close', {id:project.id}) }}">
	        Oui
	    </a>
	    <a href="{{ referer }}" class="btn btn-lg btn-primary">
	        Annuler
	    </a>
	</div>
{% endblock %}
